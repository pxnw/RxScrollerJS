{
  "name": "rx-scroll",
  "version": "0.0.2",
  "description": "RxJS Backed Scroll Listeners",
  "main": "dist/index.js",
  "author": "Marc Ammann <marc@pxnw.co>",
  "license": "MIT",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "babel src --presets babel-preset-es2015 --out-dir dist",
    "prepublish": "yarn run build"
  },
  "dependencies": {
    "rx-dom": "^7.0.3",
    "rxjs": "^5.2.0",
    "underscore": "^1.8.3"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.24.1"
  }
}
